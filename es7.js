// Includes

let numbers = [1,2,3,7,8];

if (numbers.includes(9)) {
    console.log('Number exists on array');
} else {
    console.log('Number dont exist on array');
}

// Exponential values with **

let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);